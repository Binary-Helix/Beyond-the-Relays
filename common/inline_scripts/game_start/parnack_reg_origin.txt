# Initial district counts
#	* 2× Generator Districts
#	* 2× Mining Districts
#	* 3× Agriculture Districts
# Urban Districts:
#	- 2× City Districts
# Buildings Added:
#	* Planetary Administration
# Zones:
#	* Archive Specialization
#	* Industrial Specialization

if = {
    limit = {
        AND = {
            exists = owner
            owner = {
                has_country_flag = parnack_regime_player
            }
        }
        has_planet_flag = parnack_planet
    }
    remove_all_districts = yes
    remove_all_buildings = yes
    add_building = building_capital
    add_district_and_planet_size_if_needed_effect_amount = { amount = 3 district = district_city }
    add_district_and_planet_size_if_needed_effect_amount = { amount = 2 district = district_farming }
    add_district_and_planet_size_if_needed_effect_amount = { amount = 2 district = district_generator }
    add_district_and_planet_size_if_needed_effect_amount = { amount = 2 district = district_mining }
}
else_if = {
    limit = {
        AND = {
            exists = owner
            owner = {
                has_country_flag = parnack_regime_primitive
            }
        }
        has_planet_flag = parnack_planet
    }
    add_building = building_primitive_farm
    add_building = building_primitive_factory
    add_building = building_primitive_mine
    add_building = building_primitive_power_plant
    add_building = building_primitive_capital
    add_building = building_urban_dwellings
}