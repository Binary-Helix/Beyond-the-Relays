# Number of entries controlls amount to choose from in designer  I.E.   "mol1" "mol2" "mol3" "mol4"
# graphical_culture is connected to the looks used for ships and cities, see "common/graphical_culture/"
# Portraits here are one you can choose from in the character creater. For prescripted races they are grabbed straight from the .gfx file.
# playable=(yes/no)/trigger, default is yes, if this species class can is playable
# randomized=(yes/no)/trigger, default is yes, if this species class is randomized
# custom_portraits
#	randomized = trigger, default is no, to specify if portraits are randomized
#	playable = trigger, default is yes, to specify if portraits are playable
#	portraits, list portrait keys
# species_trait_points: base number of species trait points available for this species class
# species_max_traits: maximum number of traits that species of this class can have (doesn't count those with cost == 0)
# possible: ethics/government requirements; see common/governments/readme_requirements.txt
# gender: yes|no (default: yes). whether the species have a gender
# portrait_modding: yes|no (default: no). whether the portrait can be modified with genemodding/robomodding

### MAIN ###


ASA = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_asari_01
	move_pop_sound_effect = "moving_pop_confirmation"
	gender = no
}

SAL = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_salarian_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

TUR = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_turian_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

# HUM - EDITED IN 00_species_classes.txt

ELC = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_elcor_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

VOL = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_volus_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

HAN = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_hanar_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

BAT = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_batarian_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

KRO = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_krogan_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

QUA = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_quarian_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

DRE = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_drell_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

VOR = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_vorcha_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

FOR = {
 archetype = BIOLOGICAL

 possible = {
	 authority = {
		 NOT = {
			 value = auth_machine_intelligence
			 text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
		 }
	 }
 }

 graphical_culture = btr_forayan_01
 move_pop_sound_effect = "moving_pop_confirmation"
}

LYS = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_lystheni_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

GOT = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_gothones_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

YAH = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_yahg_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

RAL = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_raloi_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

GTH = {
	archetype = MACHINE

	possible = {
		OR = {
			AND = {
				host_has_dlc = "The Machine Age"
				authority = {
					NOT = {
						value = auth_hive_mind
					}
				}
			}
			authority = {
				OR = {
					value = auth_machine_intelligence
					text = SPECIES_CLASS_MUST_USE_MACHINE_INTELLIGENCE
				}
			}
		}
	}
	possible_secondary = {
		OR = {
			AND = {
				host_has_dlc = "The Machine Age"
				civics = {
					NOT = {
						value = civic_machine_servitor
					}
				}
			}
			OR = {
				always = no
				text = SECONDARY_SPECIES_CLASS_INVALID
			}
		}
	}

	playable = {
		OR = {
			host_has_dlc = "Synthetic Dawn Story Pack"
			has_machine_age_dlc = yes
		}
	}
	randomized = {
		OR = {
			host_has_dlc = "Synthetic Dawn Story Pack"
			has_machine_age_dlc = yes
		}
		# The create_species effect can't properly take the possible trigger below into account.
		# Work around this by disabling this class for species randomization after game start.
		NOT = {
			has_global_flag = game_started
		}
	}

	trait = trait_machine_unit
	gender = no
	use_climate_preference = no
	portrait_modding = yes

	leader_age_min = 5
	leader_age_max = 10

	graphical_culture = btr_geth_01
	move_pop_sound_effect = "robot_pops_move"
	resources = {}
}

RAC = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_rachni_01
	move_pop_sound_effect = "moving_pop_confirmation"
}

COL = {
	archetype = BIOLOGICAL

	possible = {
		authority = {
			NOT = {
				value = auth_machine_intelligence
				text = SPECIES_CLASS_MUST_NOT_USE_MACHINE_INTELLIGENCE
			}
		}
	}

	graphical_culture = btr_collector_01
	move_pop_sound_effect = "moving_pop_confirmation"
}


# HSK = { 
# 	archetype = MACHINE 
# 	playable = { always = no } 
# 	portraits = { "husk" } 
# 	graphical_culture = mammalian_01 
# 	move_pop_sound_effect = "moving_pop_confirmation" 
# 	uplifted_into = "HSK" 
# 	uplifted_portraits = { "husk" } 
# 	gender = no
# }