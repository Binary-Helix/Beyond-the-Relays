antimatter_fuel_upkeep_buffer = {
	resource = antimatter_fuel
	type = upkeep
	category = buffer

	potential = {
		always = yes
	}
	weight = {
		weight = 0.1
	}
	desired_max = {
		base = 1000
	}
}

antimatter_fuel_upkeep_ships = {
	resource = antimatter_fuel
	type = upkeep
	category = ships

	potential = {
		always = yes
	}

	weight = {
		weight = 0.5
		modifier = {
			factor = 1.5
			OR = {
				is_at_war = yes
				AND = {
					used_naval_capacity_percent >= 0.9
					has_monthly_income = {
						resource = antimatter_fuel
						value < 0
					}
				}
				AND = {
					has_resource = {
						type = antimatter_fuel
						amount < 1500

					}
					used_naval_capacity_percent >= 0.8
				}
			}
		}
	}

	desired_max = { base = 5000 }
}