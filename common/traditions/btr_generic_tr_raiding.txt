tr_btr_raiding_adoption = {
    #possible = {
    #}
    unlocks_agenda = agenda_raiding
    triggered_modifier = {
        potential = {
            exists = owner
            count_enslaved_species = {  count >= 1}
        }
        modifier = {
            planet_stability_add = 5
            #TODO Make this only apply on planets with slaves
            #Officials generate influence, handled in the leaders script
        }
    }
}

tr_btr_raiding_finish = {
    modifier = {
        #custom_tooltip = btr_vigilance_finish_desc
        planet_housing_mult = 0.05
        country_naval_cap_mult = 0.2
        planet_jobs_worker_produces_mult = 0.1
        ascension_perks_add = 1
        #Also unlocks 4 special resolutions
    }
}

tr_btr_raiding_reparations = {
    #Handled through galcom.300
    modifier = {
        pop_resettlement_cost_mult = -0.5
        #TODO Make this unlock the nihilistic acquisition bombardment type
    }
    ai_weight = {
        factor = 10000
    }
}

tr_btr_raiding_prosperity = {
    possible = {
        has_tradition = tr_btr_raiding_reparations
    }
    triggered_modifier = {
        potential = {
            exists = owner
            is_at_war = yes
        }
        modifier = {
            country_minerals_produces_mult = 0.2
            country_food_produces_mult = 0.2
            country_energy_produces_mult = 0.2
            #TODO Make this only apply when at war
        }
    }

    ai_weight = {
        factor = 10000
    }
}

tr_btr_raiding_feared = {
    possible = {
        has_tradition = tr_btr_raiding_prosperity
    }
    modifier = {
        max_rivalries = 4
        intel_encryption_add = 3
        #TODO Make rival empires gain -15% happines when at war with us
    }
    #Handled through galcom.305

    ai_weight = {
        factor = 5000
    }
}

tr_btr_raiding_superiority = {
    possible = {
        has_tradition = tr_btr_raiding_prosperity
    }
    modifier = {
        army_damage_mult = 0.2
        army_collateral_damage_mult = 0.5
    }
    #Handled through galcom.310-315

    ai_weight = {
        factor = 5000
    }
}

"tr_btr_raiding_gained" = {
    possible = {
        has_tradition = tr_btr_raiding_prosperity
    }

    modifier = {
        country_war_exhaustion_mult = -0.25
        country_claim_influence_cost_mult = -0.25
        ship_fire_rate_mult = 0.1
        ship_military_upkeep_mult = -0.1
    }

    ai_weight = {
        factor = 5000
    }
}
